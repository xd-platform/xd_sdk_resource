<?xml version="1.0" encoding="utf-8"?>
<!-- GENERATED BY UNITY. REMOVE THIS COMMENT TO PREVENT OVERWRITING WHEN EXPORTING AGAIN-->
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unity3d.player"
    xmlns:tools="http://schemas.android.com/tools">


    <!-- 可选 Android 11 相关配置，在手机安装有对应 App 的情况下，无法正常跳转外部 App 进行授权登录时请参考以下配置 -->    
    <queries>
  	  <!-- TapTap -->
      	 <package android:name="com.taptap"/>
   	 <package android:name="com.taptap.pad"/>
   	 <package android:name="com.taptap.global"/>

	 <!-- Facebook -->
    	 <package android:name="com.facebook.katana"/>
  	 <!-- Facebook 有端分享 -->
 	 <provider android:authorities="com.facebook.katana.provider.PlatformProvider"/>
   	 <!-- Twitter -->
   	 <package android:name="com.twitter.android"/>
   	 <!-- LINE -->
   	 <package android:name="jp.naver.line.android"/>
   </queries>  


   
   <application>
        <!--可选：没用Facebook可以不加。 下面的 3 处 {游戏填写FBID}  需要改成游戏的 Facebook ID,  -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="{游戏填写FBID}" />
         <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:exported="true">
            <intent-filter><action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="fb{游戏填写FBID}" />
            </intent-filter>
        </activity>
        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider{游戏填写FBID}"
            android:exported="true" />

        <!-- 可选：Firebase 推送相关的配置，请注意全路径匹配-->
        <service
            android:name="com.xd.intl.common.firebase.TDSGlobalFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        
      <!--  app 启动项配置      -->
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                   android:theme="@style/UnityThemeSelector">
             <intent-filter>
                 <action android:name="android.intent.action.MAIN" />
                 <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
             <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
         </activity>
    </application>
   
</manifest>
